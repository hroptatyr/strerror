args "--unamed-opts --no-handle-error --long-help -a strerror_args_info -f strerror_parser"
package "strerror"
usage "strerror [OPTION]... ERROR"
description "Parse ERROR (an errno code or an errno literal) and print \
its expansion."

option "verbose" v
	"Output errno, the literal and the string expansion"
	optional
